package Services

func RegisterService() { // get user data and create a new user // return only error response
	// Get user data
	// check if username , email already exists
	// If not, create a new user
	// return success or error response
}
